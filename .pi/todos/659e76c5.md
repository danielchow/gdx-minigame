{
  "id": "659e76c5",
  "title": "RISK: MiniGameAudio — no audio context pooling, will hit WeChat's ~10 context limit",
  "tags": [
    "risk",
    "phase3",
    "minigame"
  ],
  "status": "done",
  "created_at": "2026-05-01T02:30:48.468Z"
}

Fixed: Replaced per-sound audio context with pool-based approach (max 10 contexts). MiniGameSound now acquires/releases contexts from a shared pool on each play()/stop(). Supports overlapping playback, unique soundIds, and auto-release on playback end. Steal-callback mechanism prevents stale references when contexts are reclaimed. MiniGameMusic unchanged (dedicated context). Build passes.
