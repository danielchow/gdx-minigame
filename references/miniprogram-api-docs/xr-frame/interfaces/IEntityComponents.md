> Source: https://developers.weixin.qq.com/miniprogram/dev/api/xr-frame/interfaces/IEntityComponents.html

[xr-frame](./../) / [Exports](./../modules.html) / IEntityComponents

## Interface: IEntityComponents

`Element`的默认组件集接口。

`name`是组件注册时的名字，`key`是要默认设置的组件的属性名字，值是默认值，但应当和`xml`中一致，为**字符串**。

## # Indexable

▪ [name: `string`]: { `[key: string]`: `string`;  }
 The translations are provided by WeChat Translation and are for reference only. In case of any inconsistency and discrepancy between the Chinese version and the English version, the Chinese version shall prevail.Incorrect translation. [Tap to report.](javascript:;)